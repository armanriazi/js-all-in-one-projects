const path = require("path");

module.exports = {
	config: path.resolve("./sequelize/config", "database.js"),
	"models-path": path.resolve("./sequelize", "models"),
	"seeders-path": path.resolve("./db", "seeders"),
	"migrations-path": path.resolve("./db", "migrations"),
};
